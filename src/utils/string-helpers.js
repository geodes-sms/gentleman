
/**
 * Preprend a string with a `.`
 * @param {string} str 
 * @returns {string}
 */
export const dot = (str) => `.${str}`;

/**
 * Preprend a string with a `#`
 * @param {string} str 
 * @returns {string}
 */
export const hash = (str) => `#${str}`;